<odoo>
    <record id="kw_partner_dob_res_partner_form" model="ir.ui.view" >
        <field name="name">res.partner.form (kw_partner_dob)</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="arch" type="xml">
            <xpath expr="//group[1]/group[2]" position="inside">
                <field name="birthday"/>
            </xpath>
        </field>
    </record>

    <record id="kw_partner_dob_res_partner_tree" model="ir.ui.view">
        <field name="name">res.partner.tree (kw_partner_dob)</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_tree"/>
        <field name="arch" type="xml">
            <tree position="inside">
                <field name="birthday" optional="show"/>
            </tree>
        </field>
    </record>
</odoo>
